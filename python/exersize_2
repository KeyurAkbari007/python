def getdate():
    import datetime
    return datetime.datetime.now()


def take(data):
    if data == 1:
        c = int(input("enter 1 for excersise and 2 for food"))
        if c == 1:
            value = input("type here\n")
            with open("keyur.txt", "w") as keyur:
                keyur.write(str([str(getdate())]))


    # print(getdate())
print("enter 1 for lock the value and enter 2 for retrive the data")
data = int(input())
if data == 1:
    b = int(input("press 1 for keyur 2 for maulik 3 for jenil"))
    take(b)
else:
    b = int(input("press 1 for keyur 2 for maulik 3 for jenil"))
    retrieve(b)
